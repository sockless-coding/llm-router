@page
@using LR.Application.Pages.Features.Servers
@model ServerCreateModel
@{
    ViewData["Title"] = "Create Server";
}

<div class="max-w-lg mx-auto">
    <form method="post" class="bg-cyber-card rounded-xl p-6 border border-neon-cyan/10 space-y-5">
        @Html.AntiForgeryToken()

        <!-- Name -->
        <div>
            <label class="block text-sm font-medium text-gray-300 mb-2">Server Name</label>
            <input asp-for="ViewModel.Name" required placeholder="e.g. cuda-main"
                   class="w-full px-4 py-2.5 rounded-lg bg-cyber-bg-dark border border-gray-600/30 text-gray-100 focus:border-neon-cyan focus:ring-1 focus:ring-neon-cyan/30 outline-none transition-all placeholder-gray-500" />
            <span asp-validation-for="ViewModel.Name" class="text-xs text-neon-red mt-1"></span>
        </div>

        <!-- Server Engine -->
        <div>
            <label class="block text-sm font-medium text-gray-300 mb-2">Server Engine</label>
            <select asp-for="ViewModel.Engine"
                    onchange="toggleEngineFields()"
                    class="w-full px-4 py-2.5 rounded-lg bg-cyber-bg-dark border border-gray-600/30 text-gray-100 focus:border-neon-cyan focus:ring-1 focus:ring-neon-cyan/30 outline-none transition-all">
                @foreach (var engine in Model.Engines)
                {
                    <option value="@engine.Value">@engine.Label</option>
                }
            </select>
        </div>

        <!-- Port -->
        <div>
            <label class="block text-sm font-medium text-gray-300 mb-2">Port <span class="text-gray-500">(optional)</span></label>
            <input asp-for="ViewModel.Port" type="number" min="1024" max="65535" placeholder="Auto-assigned"
                   class="w-full px-4 py-2.5 rounded-lg bg-cyber-bg-dark border border-gray-600/30 text-gray-100 focus:border-neon-cyan focus:ring-1 focus:ring-neon-cyan/30 outline-none transition-all placeholder-gray-500" />
        </div>

        <!-- llama.cpp-specific configuration -->
        <div id="llamacppFields" class="space-y-4">
            <h3 class="text-sm font-semibold text-gray-200 border-b border-gray-700/50 pb-2">llama.cpp Configuration</h3>

            <!-- Executable Folder Path -->
            <div>
                <label class="block text-sm font-medium text-gray-300 mb-1">
                    Server Folder Path <span class="text-neon-red">*</span>
                </label>
                <p class="text-xs text-gray-500 mb-2">Path to the folder containing your llama.cpp server executable (e.g., <code>llama-server.exe</code>). Each GPU backend build should be in its own folder.</p>
                <input asp-for="ViewModel.LlamaCppExecutableFolderPath" required placeholder="C:\llamacpp\cuda-build"
                       class="w-full px-4 py-2.5 rounded-lg bg-cyber-bg-dark border border-gray-600/30 text-gray-100 focus:border-neon-cyan focus:ring-1 focus:ring-neon-cyan/30 outline-none transition-all placeholder-gray-500 font-mono text-sm" />
                <span asp-validation-for="ViewModel.LlamaCppExecutableFolderPath" class="text-xs text-neon-red mt-1"></span>
            </div>

            <!-- Companion App Path -->
            <div>
                <label class="block text-sm font-medium text-gray-300 mb-1">
                    Companion App Path <span class="text-gray-500">(optional)</span>
                </label>
                <p class="text-xs text-gray-500 mb-2">Path to a companion application that should run alongside the server. For example, the SYCL VRAM keeper on Windows when no display is connected, or similar Vulkan helper processes.</p>
                <input asp-for="ViewModel.CompanionAppPath" placeholder="C:\tools\sycl-vram-keeper.exe"
                       class="w-full px-4 py-2.5 rounded-lg bg-cyber-bg-dark border border-gray-600/30 text-gray-100 focus:border-neon-cyan focus:ring-1 focus:ring-neon-cyan/30 outline-none transition-all placeholder-gray-500 font-mono text-sm" />
                <span asp-validation-for="ViewModel.CompanionAppPath" class="text-xs text-neon-red mt-1"></span>
            </div>
        </div>

        <!-- Actions -->
        <div class="flex items-center gap-3 pt-2">
            <button type="submit"
                    class="px-6 py-2.5 rounded-lg bg-neon-cyan/10 text-neon-cyan border border-neon-cyan/30 hover:bg-neon-cyan/20 transition-all font-medium text-sm shadow-neon-cyan">
                Create Server
            </button>
            <a href="/Features/Servers/Index"
               class="px-6 py-2.5 rounded-lg bg-gray-700/30 text-gray-400 border border-gray-600/20 hover:bg-gray-700/50 transition-all font-medium text-sm">
                Cancel
            </a>
        </div>
    </form>
</div>

@section Scripts {
<script>
function toggleEngineFields() {
    const engine = document.getElementById('ViewModel_Engine').value;
    const llamacppFields = document.getElementById('llamacppFields');
    
    if (engine === 'LlamaCpp') {
        llamacppFields.classList.remove('hidden');
    } else {
        llamacppFields.classList.add('hidden');
    }
}

// Initialize on page load
document.addEventListener('DOMContentLoaded', toggleEngineFields);
</script>
}

